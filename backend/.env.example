# PostgreSQL (recommended — use docker-compose up -d)
DATABASE_URL=postgresql://foh:foh@localhost:5432/foh

# SQLite fallback for local dev without Docker:
# DATABASE_URL=sqlite:///./foh.db

JWT_SECRET=change-me-use-a-long-random-string-in-production
JWT_EXPIRE_HOURS=8
CORS_ORIGINS=http://localhost:5173

# YOLO / camera pipeline
CAMERA_ENABLED=true
YOLO_PERSON_MODEL=yolov8n.pt
YOLO_DIRTY_MODEL_PATH=C:\Users\lucky\foh-yolo\runs\detect\foh_training\table_cleanliness_v2\weights\best.pt
DEFAULT_CAMERA_URL=C:/Users/lucky/demo_video.mp4
CAMERA_SCAN_INTERVAL_SECONDS=30
CAMERA_CLEANING_GRACE_SECONDS=60
